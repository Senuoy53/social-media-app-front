import { useState } from "react";
import Layout from "./Layout"

const SignUpPageTest = () => {
    
    return (
        <Layout title="title as prop">
                <form>
                    <p>child as prop can be a form or anything</p>
                    <h1>how do u find the layout ?</h1>
                </form>
        </Layout>
      )
}

export default SignUpPageTest;
